import React, { Component } from 'react';
import PlayerSlot from ''
const styles={
  reactionPrompt: {
    width:'100%',
    boxShadow: "0px 1px 5px #666666",
    marginTop: '40px',
  },
};

export default class ReactionPrompt extends Component {

render() {
<div style = {stles.reactionPrompt}>
{this.props.prompt}
</div>
}

}
